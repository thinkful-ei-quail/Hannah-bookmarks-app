import $ from 'jquery';

import store from './store.js';
import api from './api.js';

const generateItemElement = function(item) {
    let itemTitle = `<span class="bookmark-item bookmark-item">${item.name}`
}